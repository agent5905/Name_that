# Human acceptance failure record

## Second corrective release — implementation and release evidence

Implemented and deployed on 2026-08-12 to `https://name-that-team-member.pages.dev`.

- The saved-game definition and immutable session snapshot now carry an explicit Mystery Image, an explicit Reveal Image, and an optional plain-text Fun Fact. The editor previews and replaces both images independently; variable 2–10 answer sets and correct-answer selection remain supported.
- The client remounts room routes by identity and the snapshot hook aborts/resets by generation, ignores stale completions, terminates initial loading after 15 seconds, and retains polling/online/visibility recovery. Participant branding and ended/error states provide no normal route to host administration.
- Question and locked states use the host-authored Mystery Image. Reveal uses session-unique AES-256-GCM material: current/next ciphertext is prepared through canonical Cache API URLs, while the key is unavailable until the authoritative reveal phase. Play Again generates independent key/IV/AAD material.
- The editor clips decorative rays, constrains grid children, prevents horizontal document overflow, and keeps deliberate vertical scrolling. Fun Facts wrap safely in participant, display, and editor preview surfaces.
- Browser uploads normalize to a 400×500 RGBA PNG, matching the roughly 320–400px portrait chamber on the required 1280×720 event display while minimizing live decrypt/decode/paint work. The Worker validates size, signature, complete chunk/CRC structure, dimensions, pixel bound, deflate completion, and per-row filters without decoding and recompressing every pixel, avoiding Cloudflare error 1102 on valid large pairs.

Release evidence:

- Production migrations `202608110010` through `202608110014` are present. The live lifecycle passed explicit image pairs, immutable facts, late joins, replay isolation, wrong-key rejection, private authoritative phase pushes, capability-authenticated direct host cues, and exact cleanup.
- Local gates pass: lint, typecheck, 90 unit/static tests, production build, Functions compilation, bundle secret scan, and 21/21 mocked browser regressions.
- Deployed HTTP and browser smoke pass. Repeated real journeys proved distinct author/reveal assets, Fun Fact persistence, variable choices, Play Again, editing/replace/reorder, all late-join phases, five repeated open-round joins, no reveal dialog, Cache API HIT, session-unique replay crypto, and no plaintext reveal fallback.
- The fresh security/architecture critic returned **PASS** after verifying production migrations, cache/key isolation, private media, quota admission, rollback/cleanup behavior, and the deployed fixed bundle.
- The fresh routing/editor critic recorded 15/15 active joins in 0.923â€“1.970 s, stable editor geometry at desktop/laptop and 100%/125% zoom, explicit two-image/Fun Fact authoring, participant credential isolation, and—after one reproduced release blocker was fixed—3/3 same-tab room transitions in 1.345â€“1.858 s with zero loading nodes. Every isolated test room/game/profile/object was removed.
- The fresh image critic measured the deployed app under 4 Mbps/150 ms: Reveal painted in 268 ms on the shared display and 229 ms on the participant; the next Mystery painted in 260/79 ms. It also proved exact uploaded Mystery pixels, current/next descriptors, Cache API HIT, Fun Facts on both clients, and zero plaintext Reveal requests.
- The final event critic measured the canonical release from the host's Reveal click through the participant's first painted Reveal: **141 ms, 155 ms, and 185 ms**, passing the strict 500 ms target in all three independent rooms. The full production performance journey independently measured 368/399 ms click-to-paint and 210 ms for the next Mystery under its throttled event profile.
- The final clean-window real Pages/Supabase suite passed **11/11** serial journeys in one uninterrupted run. Its repeated throttled timing gate measured 256 ms on the shared display, 231 ms on the participant, and 290 ms for the next Mystery, all from the initiating host click.
- Isolated critic data was reconciled by exact owner/game IDs; six private test objects and three disposable game/profile graphs were removed, with zero matching rooms, games, or profiles remaining. Designed limiter windows were honored rather than bypassed.

## Second human acceptance failure — production baseline

Recorded on 2026-08-12 against `https://name-that-team-member.pages.dev` at commit `1241fe0`, before the second corrective implementation. Evidence is stored under the run-specific `routing-repro`, `image-*`, and `editor-*` visualization artifacts. Temporary admins, games, rooms, and Storage objects created for reproduction were removed and verified absent.

| Finding | Exact production reproduction | Expected | Actual | Root cause / remaining risk | Why prior checks missed it | Permanent regression |
| --- | --- | --- | --- | --- | --- | --- |
| Separate Mystery and Reveal Images | Open the editor and inspect one question and its saved API representation. | Two independently selected, previewed, replaced, and persisted images. | One **Add employee image** input and one `mediaAssetId`; zero Mystery/Reveal controls. The API silently discarded attempted `mysteryMediaAssetId` and `revealMediaAssetId` fields. | The definition stores one protected image. The server ignores its pixels and manufactures the same generic 180×225 bust as `silhouette_media_path`. | Tests asserted the one-image upload plus generated derivative contract that the product requirement now rejects. | Real editor journey uploads visibly distinct images, reloads both previews, hosts, and proves each phase renders only its assigned asset. |
| Mid-game join hydration | Open a production question and join from 15 fresh Chrome contexts; wait for the authoritative prompt/choices/image and answer without refresh. | Every late join hydrates the current state independently of Realtime. | 15/15 prompt/choice/answer flows passed in 0.818–2.513 s, but every mystery image was broken in this run. The reported intermittent blank/spinner did not reproduce. | Initial HTTP hydration exists, but room/auth changes retain snapshot, participant, loading, request, version, invalidation-gate, and queued-callback state in the hook. Old requests can suppress or overwrite a new generation under tighter timing. | Live tests used one fresh context per phase; mocked tests cannot perturb request/Re­altime order and did not repeat active joins. | Repeated active joins plus delayed/out-of-order hydration; assert HTTP snapshot renders before any broadcast and stale generations cannot commit. |
| Room A → Room B navigation | In one browser document, transition lobby→lobby, lobby→active, and finished→active with `pushState`/`popstate`, then join B. | Old channel/transient state is discarded and B reaches a terminal join/play/error state without refresh. | All three bounded runs passed in 0.511–0.602 s; the reported infinite spinner did not reproduce. The mismatch route briefly renders a spinner while an effect redirects. | Redirecting from a missing room session is effect-driven; `Join` and `useRoomSnapshot` state are not generation-keyed or synchronously reset. Rapid or delayed A/B work can cross route generations. | Existing production journeys always opened a fresh context or refreshed only after successful render; there was no same-tab A→B test. | Same-tab lobby→lobby, active→lobby, finished→active, rapid A→B→A, delayed old responses, Back/Forward, and bounded loading assertions. |
| Mystery Image absent | Host a saved game and inspect display and participant question states. | The host-provided Mystery Image is visibly rendered on both clients. | `/silhouette` returned 200 and a generic 180×225 asset in one run; in another 15-client run the image element remained `naturalWidth=0`. No user-selected Mystery Image exists at all. | Session snapshots expose only the generated `silhouette_url`; the product cannot persist or serve an explicit mystery asset. | Tests accepted any nonzero image and mocked the endpoint. They never compared uploaded pixels/identity to what production rendered on both surfaces. | Distinct fixture identity/dimensions are asserted on real display and participant clients after session snapshotting. |
| Reveal latency | Under 4 Mbps/150 ms RTT, leave a 1,540,509-byte question open for 3 s, then Reveal while recording requests and paint. | Reveal bytes are prepared before Reveal and the visual payoff begins near-immediately. | Zero reveal requests occurred before Reveal. The request began +393/+485 ms, finished +3,975/+3,875 ms, and first painted at +4,403 ms on display/participant; the chamber was blank while loading. Reload fetched the full image again because responses were `private, no-store`. | The reveal URL is introduced only after reveal state; React creates the image then, and there is no preloader or reusable asset cache. | Tests waited until `naturalWidth > 0`; they did not measure state-to-paint, throttle the network, inspect blank frames, or assert a pre-reveal preparation request. | Instrument current/next assets, verify readiness before reveal/advance, throttle realistic images, and enforce a measured state-to-visible target with graceful fallback. |
| Participant host navigation | Join, click the participant header logo, then inspect the landing page. Also call protected endpoints with the participant bearer. | Focused participant chrome has no normal host/admin route; server authorization rejects participant credentials. | Logo is `<a href="/">`; Home visibly exposes **Host studio**. Backend checks correctly returned 403 for game list, host action, and session creation. | A shared clickable `Brand` is used in participant chrome without a containment mode. | Critics checked direct links and backend auth separately, not the two-click participant escape. | Enumerate/click all participant navigation, assert the logo is inert/safe, and retain real participant-token 403 checks. |
| Editor scrollbar flicker | At 1600×1000 and 1366×768, add/remove questions and answers and upload an image while recording document dimensions. | Stable width and deliberate vertical scrolling only when content exceeds the viewport. | At 1600×1000 vertical overflow toggled off→on→off (`scrollHeight` 1000→1049→1000) and horizontal overflow appeared by 5–11 px. At 1366×768 horizontal overflow appeared/disappeared and reached 15–59 px. | Several grid descendants use intrinsic widths without `min-width: 0`; long controls plus responsive sticky/grid transitions overflow the editor container. Near-viewport content changes repeatedly cross the vertical threshold. | Screenshots were single steady states and overflow assertions covered presentation screens, not editor mutation sequences or zoom/resize. | Record `scrollWidth/clientWidth` and layout boxes through add/remove/upload/text/scroll/resize at desktop, laptop, narrow, and zoomed viewports. |
| Fun Fact missing | Inspect editor fields; POST and GET a definition containing a Fun Fact. | Optional text creates, persists, edits, clears, and renders during reveal without an empty placeholder. | No field exists; the API accepted but silently discarded `funFact`; definition/session schemas contain no saved-game fact column. | Saved sessions hard-code an empty `funFact` in revealed JSON. | Mock reveal fixtures supplied a Fun Fact that no real editor/API path could author. | Create/edit/clear/reload and immutable-session tests, plus long-text wrapping on real shared and participant reveals; render as plain text only. |

The second escape pattern was process-level: mocked image endpoints and static screenshots proved geometry, while real journeys reused fresh contexts, warm/default networks, and success-oriented waits. The revised Gauntlet must preserve request timing, same-document transitions, repeated race attempts, asset identity, failure/timeout terminal states, and deployed multi-context evidence.

## Corrective client gates

The rebuilt client now distinguishes the saved game library/editor from immutable live sessions. A host can create and edit definitions with 2–10 answers per question, select the correct answer, upload a normalized portrait, preview mystery/reveal states, host a fresh session, and use Play Again with an idempotent operation. The display, host, and participant views share the original Studio Mystery File system. The protected portrait is not requested before reveal; pre-reveal clients use only the server-provided silhouette derivative URL.

Permanent browser gates now cover library → host, editor draft/media/final update, varying answer counts, late-join eligible denominators, immediate reveal, Play Again retry identity, protected portrait release, presentation sizes, and mobile submitted/locked states. Screenshot review covers library, editor, lobby, question, locked, reveal, results, complete, host control, and participant phone layouts.

Recorded on 2026-08-11 against `https://name-that-team-member.pages.dev` at commit `e0f885d` before corrective implementation.

The prior release passed unit, mocked browser, live API, load-boundary, and deployment checks, yet it did not cover the product lifecycle a host actually needs. These six findings are the baseline for the replacement Gauntlet.

| Finding | Reproduction and observed behavior | Expected behavior | Root cause | Missing regression / why the prior Gauntlet missed it |
| --- | --- | --- | --- | --- |
| Cannot play again | Complete the final round and select **Finish game**. The host panel becomes “Game complete” with no action, library link, or fresh-session path. Reload restores the same completed room. | **Play Again** creates a new room with clean players, answers, phase, and round while reusing saved content. | The data model has rooms but no reusable game definition. The host stores only one room credential, and the complete-state UI has no next action. | Existing Playwright asserted that **Finish game** was visible in a mocked final state; it never finished a real session and attempted a second one. |
| Content cannot be managed | Open `/host`. The only content action is **Create a new game**, which immediately creates a live room from four globally seeded employees. There is no library, editor, question/choice control, upload, preview, save, edit, or delete UI. | A normal user can create, persist, edit, reorder, preview, host, and delete reusable games. | `employees` is global seed content. `create_room` builds one round per active employee and selects exactly four choices. There are no game-definition tables or CRUD/upload endpoints. | Tests began after supplying mocked room snapshots; none started from a clean host library or attempted content authoring. |
| Joining fails after lobby | A temporary production room accepted a lobby join (`201`). First-time joins during `question_open`, `answers_locked`, `employee_revealed`, and `complete` all returned `409 ROOM_NOT_JOINABLE` with “Room is no longer accepting participants.” Direct `/join/:code` and manual code entry converge on the same endpoint. | Active rooms accept late participants and synchronize them to the current phase; only a completed room shows a specific ended message. | `join_room` explicitly requires `phase = 'lobby'`. The single error does not distinguish active locked/reveal states from completion. | The browser join test mocked a lobby-only `201`. Live integration joined both players before start. No multi-context late-join journey existed. |
| Reveal interrupts progression | Start, lock, and press **Reveal teammate**. A browser confirmation asks “Reveal the teammate on every screen?” before the state changes. | Reveal is an immediate, state-aware presentation action with no routine dialog. | `HostDashboard.act` calls `window.confirm` for both `reveal` and `end`. | The host tests mocked state and either did not click Reveal or accepted the dialog as normal. No test asserted that Reveal creates zero dialogs. |
| Presentation feels like a poll | Inspect lobby, question, reveal, results, host, and mobile states. The restrained cream/forest editorial system is legible, but motion is limited to a short reveal entrance and result bars; mystery framing lacks a bold show-title beat, saturated burst composition, suspense transition, and celebratory payoff. | An original, IP-safe nostalgic mystery/reveal show with energetic silhouette, pacing, framing, motion, and cohesive mobile/host/display language. | The visual system optimized corporate polish/readability. Critics were asked mainly about fit, decode, tap size, and legibility rather than whether the experience was emotionally game-like. | Screenshot tests checked dimensions, QR decoding, portrait loading, and overflow. The visual question “game show or generic polling?” was not a binary gate. |
| No saved-game / hosting flow | Visit `/host`: there is no game library. Creating a room permanently couples a one-off session to current global content. Editing content later would mutate shared employee rows referenced by prior rounds. | Saved Game → many independent Session snapshots; edits affect future sessions only. | The schema has `employees`, `rooms`, `rounds`, and choices, but no owner-scoped saved games/questions/answers. Session rounds reference mutable global employee records instead of immutable copied question content. | Architecture review validated room-state authorization and concurrency but never evaluated reusable definitions, ownership, or repeat hosting. |

## Revised permanent gates

- Browser tests must exercise the real Pages Functions and Supabase backend for authoring, hosting, completing, replaying, editing, and late joining; mocked visual tests remain supplemental only.
- A saved game must produce multiple fresh session IDs/codes without carrying players, answers, results, phase, or Realtime topics forward.
- First-time join is tested independently in lobby, open, locked, reveal, results, and complete phases.
- Reveal fails if an alert, confirm, or modal is emitted.
- Content tests cover variable question and answer counts, reordering, replacement uploads, reload persistence, and immutable active-session snapshots.
- Fresh critics receive only the production URL and a human goal. Visual acceptance explicitly fails a generic dashboard/polling experience.
